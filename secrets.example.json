{
  "_comment": "Copy this file to secrets.local.json (gitignored) and fill in real values. Only needed for the optional PFF grades reference field — everything else in build_board_data.py works with no secrets at all.",
  "pff_token": "PASTE_YOUR_X-PFF-TOKEN_HERE"
}
